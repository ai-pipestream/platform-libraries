# Test infrastructure services for pipeline-messaging integration tests
# Uses Redpanda (Kafka-compatible) and Apicurio Registry

name: pipeline-kafka-it

networks:
  kafka-it-network:
    driver: bridge

volumes:
  mysql_it_data:
    driver: local

services:
  # MySQL instance for Apicurio Registry storage
  mysql-test:
    container_name: kafka-it-mysql
    image: mysql:8.0
    networks:
      - kafka-it-network
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: apicurio_registry
      MYSQL_USER: pipeline
      MYSQL_PASSWORD: password
    command:
      # Performance optimizations for test environment
      - --innodb-flush-log-at-trx-commit=0
      - --innodb-doublewrite=0
      - --skip-log-bin
      - --performance-schema=0
      - --innodb-buffer-pool-size=256M
    ports:
      - "3308:3306"
    volumes:
      - mysql_it_data:/var/lib/mysql
    labels:
      quarkus-dev-service-mysql: kafka-it
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 3s
      timeout: 5s
      retries: 10
      start_period: 10s

  # Redpanda for Kafka-compatible messaging
  kafka-test:
    container_name: kafka-it-redpanda
    image: redpandadata/redpanda:latest
    hostname: kafka-test
    networks:
      - kafka-it-network
    command:
      - redpanda start
      - --smp 1
      - --overprovisioned
      - --kafka-addr internal://0.0.0.0:9092,external://0.0.0.0:9094
      - --advertise-kafka-addr internal://kafka-test:9092,external://${DOCKER_GATEWAY_HOST:-172.17.0.1}:9095
    ports:
      - "9095:9094"
    labels:
      quarkus-dev-service-kafka: kafka-it
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy:.*true'"]
      interval: 3s
      timeout: 10s
      retries: 10
      start_period: 5s

  # Apicurio Registry for protobuf schema management
  apicurio-registry-test:
    container_name: kafka-it-apicurio
    image: apicurio/apicurio-registry:3.0.12
    networks:
      - kafka-it-network
    ports:
      - "8082:8080"
    depends_on:
      mysql-test:
        condition: service_healthy
    environment:
      APICURIO_DATASOURCE_URL: 'jdbc:mysql://mysql-test:3306/apicurio_registry'
      APICURIO_DATASOURCE_USERNAME: 'pipeline'
      APICURIO_DATASOURCE_PASSWORD: 'password'
      APICURIO_SQL_INIT: 'true'
      APICURIO_STORAGE_KIND: 'sql'
      APICURIO_STORAGE_SQL_KIND: 'mysql'
      QUARKUS_HTTP_PORT: 8080
      QUARKUS_LOG_LEVEL: 'INFO'
      LOG_LEVEL: 'INFO'
      QUARKUS_HTTP_CORS_ORIGINS: '*'
      QUARKUS_PROFILE: 'prod'
    labels:
      io.quarkus.devservices.compose.wait_for.logs: ".*Listening on.*"
      io.quarkus.devservices.compose.config_map.8080: mp.messaging.connector.smallrye-kafka.apicurio.registry.url
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
